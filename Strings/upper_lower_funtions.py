# This example shows how to use upper() and lower() functions
# upper() and lower() return new strings because strings are immutable

data = "dATa"

print(f"Uppercase: {data.upper()}")
print(f"Lowercase: {data.lower()}")
